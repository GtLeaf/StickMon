# Caves 地形块语义

本表记录 `Pokemon Essentials/Graphics/Tilesets/Caves.png` 中已经人工确认的常规 tile 用途。编号遵循 RPG Maker XP 规则：`tile_id = 384 + source_row * 8 + source_column`。完整 Caves 编号图位于 `origin_asset/generated/game/tileset_reference/caves/`。

霜晶冰窟使用独立的完整候选目录 `origin_asset/generated/game/tileset_reference/frost_crystal_cave/`：

- 连续收录 `1120～1543`，共 424 块，不省略未知块和红叉占位。
- `1120～1239` 暂列为冰岩候选，`1240～1543` 列为核心雪地/冰面候选。
- `CONF` 表示人工确认；`INFER` 表示视觉或元数据推定；`M34?` 表示 Map034 已使用但用途未知；`UNK` 表示完全待确认；`EMPTY` 表示红叉占位或空槽。
- 每块同时标出 Map034 使用次数、所在图层计数、passage、priority 和 terrain tag，便于继续逐项确认。
- 旧的 `frost_crystal_cave_prototype/frost_crystal_cave_tile_guide.png` 只是精选子集，不再作为完整目录。

## 3x3 岩石小岛

```text
552  553  554
560  561  562
568  569  570
```

- `561` 是中心可扩展区域。
- `552/553/554/560/562/568/569/570` 是八个方向的小岛边缘。
- 九块合用组成最小的完整岩石小岛；扩展小岛时保留外圈方向，在内部重复 `561`。

## 悬崖走向

- `571`：悬崖走向从右侧拐为向下。
- `572`：悬崖走向从左侧拐为向下。

## 山洞入口与出口

- `515`：朝向左侧的山洞口。
- `517`：朝向右侧的山洞口，已人工确认。
- `524+532`：组合成朝向屏幕的山洞口。
- `526`：背向屏幕的山洞口。
- 上述山洞口均可作为地图入口或出口；进入后由地图转场事件决定目标。

## 台阶与梯子

- `539`：山洞内的岩石台阶。
- `540+548`：爬到上一层的梯子。当前试作按 `540` 在上、`548` 在下组成两格竖向模块。
- `541`：下行梯口的上层图块，不能单独使用；仍叠放在中心 `610` 的 Layer 1。底层模块的顶部封边根据相邻地形二选一：

  - 梯口顶部是小岛边缘或岩石块边缘时，顶部直接使用 `601/602/603`。
  - 梯口顶部不是上述岩石边缘时，顶部使用 `617/618/619` 的 Y 轴镜像。

  当前语义模板记为：

  ```text
  top-left  top  top-right = 601/602/603
                           or flipY(617/618/619)
  609 [610] 611
  617  618  619
  ```

- 生成时中心 Layer 0 仍为 `610`，再把 `541` 写入同一坐标的 Layer 1。禁止用 `541` 替换 `610`；顶部封边必须作为完整三格选择，不能混用两种方案。
- 运行时使用独立别名 `4729～4740` 保存这套 12 块底层资源：`4729～4737` 对应 `601～619`，`4738～4740` 对应 `617～619` 的 Y 轴镜像版本。运行时中心格使用 `4733+4708`，而不是把原始 `541` 直接当作 Outside 图集编号。
- 旧的 Python 预览别名 `4610/4611/4612` 仅保留作历史候选图兼容，不再作为运行时资源编号；当前随机霜晶冰窟使用 `4747`（来源 `1333`）作为下行楼梯。普通 Cave 梯子底座和 `541` 叠加模块已登记并可渲染，但尚未在霜晶冰窟中随机投放，避免棕绿色洞穴底图与冰窟配色混用。
- 九格底层必须全部落在可通行岛面，不得覆盖水面、悬崖或桥；中心 `610+541` 必须落在路线上，3x3 轮廓至少一格接触所声明的入口或出口边界。具体交互与转场方式可在地图视觉确认后再定。
- 梯子格必须位于可通行地面和主路上，不得覆盖水面、悬崖或桥。用作边界出入口时，梯子至少有一格接触屏幕边界。

## 霜晶冰窟交互块

### 光滑冰面

- 运行时中心冰面 `4504` 与边缘冰面 `4524～4531` 统一属于光滑冰；边缘块只是自动拼接轮廓，不改变碰撞和滑行语义。
- 队首从普通地面踏入光滑冰时锁定进入方向，并以每格 `180ms` 的线性恒速连续滑行。滑行期间 A/B 不改变速度、方向，也不能打开探索菜单。
- 冰面路线只能沿进入方向直穿，不能在冰面中转弯、分叉或结束。正常路线应在最后一格冰面后保留一格普通地面作为停靠区；若前方没有合法同向路线或遇到阻挡，则停在当前冰格。
- 滑行经过的每格照常累计步数、消耗喷雾、递减战斗冷却并结算队伍步数恢复；随机战斗、保底战斗、物品和 Boss 必须延后到离开冰面后结算。生成事件位置时统一迁移到合法范围内最近的非冰面路线点。
- 冰面连续段由 Python/C++ 双端生成器共同验证：每个冰面路线点必须同时具有同方向的入边和出边，分叉点必须位于非冰面。修改模板后必须保留该验证。

### 雪地岩壁与凹角

Map034 使用下面的 3x3 模块组成不可进入的积雪岩块：

~~~text
1296  1297  1298
1304  1305  1306
1312  1313  1314
~~~

- `1305` 是积雪填充；上下左右和四个角共同封闭整块地形，不能只用直线墙边代替拐角。
- 当可通行地面包围一个对角方向的积雪缺口时，生成器必须额外写入凹角：缺口在左上用 `1314`，右上用 `1312`，左下用 `1298`，右下用 `1296`。
- 凹角检测条件是两个相邻正交格均为地面、对角格为外部，并且当前格没有普通边墙。凹角块按 Map034 的 `passage=15` 处理为不可通行。
- `1318/1319` 不是通用凹角；它们与下方的 `1326/1327` 共同参与圆形水面模块，禁止拿来补普通墙角。

### 3x3 冰岩小岛

透明底版：

~~~text
1120  1121  1122
1128  1129  1130
1136  1137  1138
~~~

自带背景版：

~~~text
1123  1124  1125
1131  1132  1133
1139  1140  1141
~~~

- 两组都是不可拆分的 3x3 岩石小岛模块，禁止单独随机摆放边角或中心块。
- `1120` 组带透明区域，必须先根据场景语义选择 Layer 0，再将小岛写入 Layer 1；底层可以是雪地、洞穴地面、水面或冰面，不强制绑定冰面。
- `1123` 组已包含背景像素，不依赖冰面底图；仍须按完整 3x3 写入，并且不得与透明版混拼。其背景色与当前“霜晶冰窟”的雪地、冰面配色不匹配，当前场景生成器暂时禁用该组。
- “小岛”是该九格岩石轮廓的模块名，不表示它只能出现在水面或冰面；放在雪地、洞穴地面时可作为大型岩丘或不可进入的岩石台地。
- 一次探索只要求整套地图具有景观变化，不要求每张地图都出现岩石小岛或冰面；应在不同地图间分配纯雪地、冰廊、岩丘和晶石区，避免四张图使用同一套景观清单。
- Map034 元数据中两组的 18 块均为 `passage=15, priority=0, terrainTag=4`；在 StickMon 生成器中暂将整个 3x3 区域标记为不可进入的岩石景观。

- `1299/1300/1301`：横向三格组合成一个洞穴出口，不应拆开随机摆放。

  ```text
  1299  1300  1301
  ```

- 运行时别名为 `4741/4742/4743`，三格必须在同一 Layer 1 连续写入。霜晶生成器只在顶部边界、且三格不会覆盖另一条路线时使用它；其他方向沿用完整雪墙开口语义。

- `1321`：碎裂中的冰面；`1322`：`1321` 继续碎裂后形成的洞。两者是前后状态，不是两种随机装饰。
- `1326`：一滩圆形水的左下角块。
- `1327`：一滩圆形水的右下角块。
- `1333`：通向下一层的楼梯。
- `1341`：洞。

- 运行时别名为：`1322 -> 4744`、`1326/1327 -> 4745/4746`、`1333 -> 4747`、`1341 -> 4748`。
- `1322`、`1341` 属于状态/事件型块，当前只登记资源和语义，不由随机装饰阶段生成；必须由冰面碎裂或洞穴事件显式触发。`1326/1327` 也必须与其余圆形水面模块一起组合，不能单独撒放。

## 半格与四分之一格边缘

这组块共同表达一条连续边缘，不得拆开随机摆放：

```text
579 -> 580              582 -> 583
        |                 ^
       588               590
        |                 ^
       596 -> 597 -> 598
```

- `580`：从向右改为向下的拐点。
- `596`：从向下改为向右的拐点。
- `598`：从向右改为向上的拐点。
- `582`：从向上改为向右的拐点。
- 完整顺序为 `579,580,588,596,597,598,590,582,583`。

冰窟中的 `1344/1345/1353/1361/1362/1363/1355/1347/1348` 与上述 `579` 系列表达相同的连续边缘走势：

```text
1344 -> 1345              1347 -> 1348
          |                 ^
         1353              1355
          |                 ^
         1361 -> 1362 -> 1363
```

- `1345`：从向右改为向下的拐点。
- `1361`：从向下改为向右的拐点。
- `1363`：从向右改为向上的拐点。
- `1347`：从向上改为向右的拐点。
- 完整顺序为 `1344,1345,1353,1361,1362,1363,1355,1347,1348`；同样不得拆开随机摆放。

- 运行时别名 `4749～4757` 一一对应上述九块；当前已进入地图包，可用于后续连续岩边模板，但尚未作为普通随机装饰逐块投放。

## 运行时 Cave 资源边界

固件使用一个合并的探索 tile 图集。Outside、Sea 和 Caves 的原始 RPG Maker 编号会相互碰撞，因此 Caves 资源不直接使用原编号，而使用连续的运行时别名 `4700～4761`。映射如下：

| 运行时别名 | Caves 来源 | 用途 | 当前状态 |
| --- | ---: | --- | --- |
| `4700/4701` | `515/517` | 左/右山洞入口 | 已打包，事件预留 |
| `4702/4703` | `524/532` | 朝向屏幕的两格入口 | 已打包，事件预留 |
| `4704` | `526` | 背向屏幕入口 | 已打包，事件预留 |
| `4705` | `539` | 岩石台阶 | 已打包，事件预留 |
| `4706/4707` | `540/548` | 上行梯子两格 | 已打包，事件预留 |
| `4708` | `541` | 下行梯子 Layer 1 叠加 | 已打包，必须叠加底座 |
| `4709～4717` | `552～570` | 3x3 透明岩石小岛 | 已打包，完整九格使用 |
| `4718/4719` | `571/572` | 悬崖左右转下拐角 | 已打包，连续岩边使用 |
| `4720～4728` | `579～598` | 连续半格/四分之一格岩边 | 已打包，按完整走势使用 |
| `4729～4737` | `601～619` | 下行梯子岩石底座 | 已打包，完整 3x3 使用 |
| `4738～4740` | `617～619` Y 轴镜像 | 非岩石顶部的下行梯子封边 | 已打包，完整三格使用 |
| `4741～4743` | `1299～1301` | 霜晶冰窟三格出口 | 已在随机生成器启用 |
| `4744` | `1322` | 碎裂后的冰洞 | 事件型预留 |
| `4745/4746` | `1326/1327` | 圆形水面下侧左右角 | 组合模块预留 |
| `4747` | `1333` | 霜晶冰窟下行楼梯 | 已在随机生成器启用 |
| `4748` | `1341` | 洞穴 | 事件型预留 |
| `4749～4757` | `1344～1363` | 霜晶连续岩边走势 | 已打包，模板预留 |
| `4758` | `513` | 古木瀑谷 Cave 地面/岩台中心 | 已在区域 5 启用 |
| `4759` | `576` | 古木瀑谷 Cave 悬崖左缘 | 已在区域 5 启用 |
| `4760` | `577` | 古木瀑谷 Cave 悬崖中段 | 已在区域 5 启用 |
| `4761` | `578` | 古木瀑谷 Cave 悬崖右缘 | 已在区域 5 启用 |

当前随机霜晶冰窟只从已确认并适配雪地配色的模板生成雪地、完整雪墙、冰面、晶石、雪石、三格顶部出口和下行楼梯；透明 3x3 岩丘 `4532～4540` 已从运行时霜晶模板移除。入口四方向仍由雪墙开口与路线拓扑表达；普通 Cave 入口图块、岩石小岛、普通梯子、碎冰洞、圆形水面和连续岩边不能被当作孤立随机装饰。所有新增模块必须先定义完整组合、Layer、阻挡和转场语义，再加入 Python/C++ 双端生成器。

区域 5“古木瀑谷”使用 `4758` 作为 Layer 0 的 Cave 地面，瀑布两侧不可进入的悬崖按 `4759/4760/4761` 从屏幕左缘、中段到右缘连续选择；楼梯位置使用已确认的 `4705` 岩石台阶。入口和出口使用 `4700/4701` 左右洞口、`4702/4703` 朝向屏幕洞口或 `4704` 背向屏幕洞口，写入 Layer 1。古木瀑谷不再混用 Outside 草地、绿色道路、顶边森林或普通草花灌木；Sea/Waterfall 仍保留为水面和瀑布动画资源。区域 3“霜晶冰窟”只使用 `4500～4544` 雪地专用别名和 `4741～4757` 已确认的雪窟事件/边缘模块，不使用通用 Cave `4700～4740`，也不使用 `4758～4761`。Python 与 C++ 必须同步这些资源边界。

## 当前梯子与运行时验收

- “左瀑三岛 01”在底部入口使用 `540+548` 向上梯子。
- 霜晶冰窟 v9 使用固定种子 `0x2` 生成四张验收图，覆盖横向/纵向房型、两种直穿冰面路线，并在可用的顶部出口写入完整 `4741/4742/4743` 模块、在底部出口写入 `4747`。
- 既有 v8 预览目录 `origin_asset/generated/game/runtime_frost_crystal_cave_v8/` 保留作历史对照；当前 v9 规则以 Python/C++ 运行时生成器和测试为准，其中 `runtime_tile_maps.png` 是总览，`*_debug.png` 是路线、阻挡和出口调试图。
- Python 与 C++ 共享算法版本 `9`、候选顺序、随机数调用顺序和 FNV 指纹；修改本表中任何运行时模块后，必须重新运行 `tools/test_generate_runtime_tile_map.py` 与 `tools/test_explore_ice_slide.py` 并恢复双端指纹及滑行测试通过。
